
name: Build and Deploy
on: 
  push:
    braches:
      - main
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Install Dep
      run: npm install

      - name: Build
      run: npm run build


